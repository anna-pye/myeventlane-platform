# Site-specific service overrides for all environments.
#
# Twig debug must not live here — it is loaded via development.services.yml on
# DDEV only (see settings.mel_shared_session.php).
services: {}
