{#
/**
 * @file
 * Dependency-light branded 404 page.
 */
#}
{{ attach_library('mel_maintenance/errors') }}

<main id="main-content" tabindex="-1" class="mel-err mel-err--404" data-mel-error-page>
  <div class="mel-err__decoration" aria-hidden="true"></div>
  <section class="mel-err__panel" aria-labelledby="mel-404-title">
    <img class="mel-err__character" src="{{ base_path }}{{ directory }}/images/mel-thinking.png" width="107" height="179" alt="">
    <p class="mel-err__code" aria-hidden="true">404</p>
    <p class="mel-err__eyebrow"><img class="mel-err__logo" src="{{ base_path }}{{ directory }}/logo.svg" width="200" height="80" alt="MyEventLane"></p>
    <h1 id="mel-404-title" class="mel-err__title">{{ "We couldn't find that page"|t }}</h1>
    <p class="mel-err__lede">
      {{ 'The link may be out of date, or the page may have moved.'|t }}
    </p>
    <div class="mel-err__actions">
      <a class="mel-err__btn mel-err__btn--primary" href="{{ path('view.upcoming_events.page_events') }}">{{ 'Browse events'|t }}</a>
      <a class="mel-err__btn mel-err__btn--secondary" href="{{ path('<front>') }}">{{ 'Go to homepage'|t }}</a>
    </div>
    <p class="mel-err__support">
      {{ 'Looking for a booking?'|t }}
      <a href="{{ path('myeventlane_support.page') }}">{{ 'Contact support'|t }}</a>
    </p>
  </section>
</main>
