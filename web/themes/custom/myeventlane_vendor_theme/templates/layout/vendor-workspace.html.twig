{#
/**
 * @file
 * @deprecated Phase 2A — unused legacy 3-column workspace shell.
 *
 * No theme hook, preprocess, or include references this template in the
 * repository. Canonical page wrapper is layout/page.html.twig (VendorShell).
 * CSS class .mel-vendor-workspace remains in workspace.scss for Studio layout.
 *
 * Do not remove until Phase 2B ownership review. See docs/vendor-experience-v3-audit.
 *
 * Shared MEL vendor workspace shell (legacy).
 */
#}
<header class="mel-topbar"></header>

{% set sidebar_content = sidebar|default('') %}

<div class="mel-vendor-workspace mel-workspace{% if sidebar_content is not empty %} mel-workspace--with-sidebar{% endif %}">
  {% if sidebar_content is not empty %}
    <aside class="mel-sidebar">
      {{ sidebar_content }}
    </aside>
  {% endif %}

  <main class="mel-vendor-main mel-canvas">
    {{ content|default('') }}
  </main>

  <aside class="mel-vendor-insights mel-inspector">
    {{ insights|default('') }}
  </aside>
</div>
