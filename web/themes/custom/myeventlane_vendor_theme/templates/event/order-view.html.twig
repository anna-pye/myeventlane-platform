{#
  Order detail (Humanitix-style). Read-only.
  Variables: event, order_number, back_url, purchaser, payment, tickets, attendees, order_view_url, refund_timeline.
#}
<div class="mel-order-view mel-sales-ops">
  {# 1. Back link #}
  <p class="mel-order-view__back">
    <a href="{{ back_url }}" class="mel-link mel-link--back">{{ '← Back to orders'|t }}</a>
  </p>

  <nav class="mel-sales-ops__nav" aria-label="{{ 'Event sales tools'|t }}">
    <a class="mel-sales-ops__nav-item is-active" href="{{ orders_url }}" aria-current="page">
      <strong>{{ 'Orders'|t }}</strong>
      <small>{{ 'Return to every completed purchase.'|t }}</small>
    </a>
    {% if addon_orders_url %}
      <a class="mel-sales-ops__nav-item" href="{{ addon_orders_url }}">
        <strong>{{ 'Add-on orders'|t }}</strong>
        <small>{{ 'Prepare merchandise and extras.'|t }}</small>
      </a>
    {% endif %}
    {% if refunds_url %}
      <a class="mel-sales-ops__nav-item" href="{{ refunds_url }}">
        <strong>{{ 'Refunds'|t }}</strong>
        <small>{{ 'Review buyer refund requests.'|t }}</small>
      </a>
    {% endif %}
  </nav>

  {# 2. Order header card #}
  <div class="mel-card mel-order-view__header">
    <div class="mel-card__header mel-card__header--no-border">
      <div>
        <h2 class="mel-order-view__title">{{ 'Order'|t }} {{ order_number }}</h2>
        <span class="mel-badge mel-badge--{{ purchaser.order_status|lower }}">{{ purchaser.order_status }}</span>
      </div>
      <div class="mel-order-view__date">{{ purchaser.order_date }}</div>
    </div>
    <div class="mel-order-view__header-help">
      <span>{{ 'Use this record to answer a guest, check who the tickets belong to, or review previous refund activity.'|t }}</span>
      {% if help_url %}<a href="{{ help_url }}">{{ 'Open the sales guide'|t }}</a>{% endif %}
    </div>
  </div>

  {# 3. Purchaser summary card #}
  <div class="mel-card mel-order-view__card">
    <div class="mel-card__header">
      <div>
        <div class="mel-card__title">{{ 'Purchaser'|t }}</div>
        <p class="mel-order-view__card-help">{{ 'The person who placed and paid for the order. Ticket holders can be different people.'|t }}</p>
      </div>
    </div>
    <div class="mel-card__body">
      <dl class="mel-order-view__dl">
        <dt>{{ 'Name'|t }}</dt>
        <dd>{{ purchaser.purchaser_name }}</dd>
        <dt>{{ 'Email'|t }}</dt>
        <dd>{{ purchaser.purchaser_email }}</dd>
      </dl>
    </div>
  </div>

  {# 4. Payment summary (subtotal, fees, refunds, total) #}
  <div class="mel-card mel-order-view__card">
    <div class="mel-card__header">
      <div>
        <div class="mel-card__title">{{ 'Payment summary'|t }}</div>
        <p class="mel-order-view__card-help">{{ 'Event-linked sales, fees and completed refunds are shown separately for easier reconciliation.'|t }}</p>
      </div>
    </div>
    <div class="mel-card__body">
      <dl class="mel-order-view__dl mel-order-view__payment">
        <dt>{{ 'Subtotal'|t }}</dt>
        <dd>{{ payment.subtotal }}</dd>
        <dt>{{ 'Fees'|t }}</dt>
        <dd>{{ payment.fees }}</dd>
        <dt>{{ 'Refunds'|t }}</dt>
        <dd>{{ payment.refunds }}</dd>
        <dt class="mel-order-view__total-dt">{{ 'Total'|t }}</dt>
        <dd class="mel-order-view__total-dd">{{ payment.total }}</dd>
      </dl>
    </div>
  </div>

  {% if refund_timeline %}
    <div class="mel-card mel-order-view__card mel-order-view__refunds" aria-label="{{ 'Refunds'|t }}">
      <div class="mel-card__header">
        <div>
          <div class="mel-card__title">{{ 'Refund history'|t }}</div>
          <p class="mel-order-view__card-help">{{ 'A refund is not complete until its status says completed. Failed attempts can be retried safely from Orders.'|t }}</p>
        </div>
      </div>
      <div class="mel-card__body">
        {{ refund_timeline }}
      </div>
    </div>
  {% endif %}

  {# 5. Tickets summary card #}
  <div class="mel-card mel-order-view__card">
    <div class="mel-card__header">
      <div>
        <div class="mel-card__title">{{ 'Tickets and event items'|t }}</div>
        <p class="mel-order-view__card-help">{{ 'This section shows the order lines that belong to this selected event.'|t }}</p>
      </div>
    </div>
    <div class="mel-card__body">
      {% if tickets|length > 0 %}
        <div class="mel-table mel-table--striped">
          <div class="mel-table__head">
            <div>{{ 'Ticket'|t }}</div>
            <div>{{ 'Unit price'|t }}</div>
            <div>{{ 'Qty'|t }}</div>
            <div>{{ 'Line total'|t }}</div>
          </div>
          <div class="mel-table__body">
            {% for row in tickets %}
              <div class="mel-table__row">
                <div>{{ row.ticket_name }}</div>
                <div>{{ row.unit_price }}</div>
                <div>{{ row.quantity }}</div>
                <div>{{ row.line_total }}</div>
              </div>
            {% endfor %}
          </div>
        </div>
      {% else %}
        <p class="mel-text--muted">{{ 'No ticket lines for this event.'|t }}</p>
      {% endif %}
    </div>
  </div>

  {# 6. Attendees list (cards) #}
  <div class="mel-order-view__attendees">
    <h3 class="mel-order-view__section-title">{{ 'Attendees'|t }}</h3>
    <p class="mel-order-view__section-help">{{ 'These are the named ticket holders. Use their ticket code when helping with entry or a ticket-specific refund.'|t }}</p>
    {% if attendees|length > 0 %}
      <div class="mel-order-view__attendee-list">
        {% for a in attendees %}
          <div class="mel-card mel-order-view__attendee-card">
            <div class="mel-card__body">
              <dl class="mel-order-view__dl">
                <dt>{{ 'Name'|t }}</dt>
                <dd>{{ a.full_name|default(((a.first_name|default('')) ~ ' ' ~ (a.last_name|default('')))|trim) }}</dd>
                <dt>{{ 'Email'|t }}</dt>
                <dd>{{ a.email }}</dd>
                <dt>{{ 'Phone'|t }}</dt>
                <dd>{{ a.phone|default('—') }}</dd>
                <dt>{{ 'Ticket code'|t }}</dt>
                <dd>{% if a.ticket_code %}{{ a.ticket_code }}{% else %}—{% endif %}</dd>
                <dt>{{ 'Ticket option'|t }}</dt>
                <dd>{{ a.ticket_type }}</dd>
                {% if a.custom_answers is defined %}
                  {% for row in a.custom_answers %}
                    <dt>{{ row.label }}</dt>
                    <dd>{{ row.value }}</dd>
                  {% endfor %}
                {% else %}
                  {% for q in a.custom_questions|default([]) %}
                    <dt>{{ q.label }}</dt>
                    <dd>{{ q.value }}</dd>
                  {% endfor %}
                {% endif %}
              </dl>
              <p class="mel-order-view__link-back">
                <a href="{{ a.order_view_url }}" class="mel-link">{{ 'View order'|t }}</a>
              </p>
            </div>
          </div>
        {% endfor %}
      </div>
    {% else %}
      <p class="mel-text--muted">{{ 'No attendee data for this event’s tickets.'|t }}</p>
    {% endif %}
  </div>
</div>
