{#
/**
 * @file
 * Event node — event_card_compact (horizontal compact card).
 */
#}
{% set is_past = mel_event_state is defined and mel_event_state in ['ended', 'archived'] %}
{% include '@myeventlane_theme/components/event-card/mel-event-card.html.twig' with {
  variant: 'compact',
  is_past: is_past,
  attributes: attributes,
} %}
