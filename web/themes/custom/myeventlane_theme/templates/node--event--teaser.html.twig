{#
/**
 * @file
 * Event node — teaser view mode (standard card).
 *
 * Delegates to the canonical mel-event-card component; card_* variables
 * are set in myeventlane_theme_preprocess_node() for event bundles.
 */
#}
{% include '@myeventlane_theme/components/event-card/mel-event-card.html.twig' with {
  variant: 'standard',
  attributes: attributes,
} %}
