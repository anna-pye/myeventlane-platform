{#
/**
 * @file
 * Theme override for Commerce empty cart page.
 *
 * MEL empty state: gradient visual (no dependency on raster art), trust nav, recovery CTA.
 *
 * @see commerce/modules/cart/templates/commerce-cart-empty-page.html.twig
 */
#}
<div class="mel-cart-empty">
  <div class="mel-cart-empty__visual" aria-hidden="true"></div>
  <h1 class="mel-cart-empty__title">{{ 'Your cart is empty'|t }}</h1>
  <p class="mel-cart-empty__text">{{ "When you're ready, browse events and add tickets in a few taps."|t }}</p>
  <a href="{{ path('view.upcoming_events.page_events') }}" class="mel-cart-empty__cta">{{ 'Browse events'|t }}</a>
  <nav class="mel-checkout-trust-nav mel-cart-trust-nav mel-cart-empty__support" aria-label="{{ 'Help and support'|t }}">
    <a class="mel-checkout-trust-nav__link" href="{{ path('myeventlane_help_centre.home') }}">{{ 'Help Centre'|t }}</a>
    <span class="mel-checkout-trust-nav__sep" aria-hidden="true">·</span>
    <a class="mel-checkout-trust-nav__link" href="{{ path('myeventlane_support.page') }}">{{ 'Support'|t }}</a>
  </nav>
</div>
