{#
/**
 * @file
 * Template for mel_home_events View wrapper.
 * 
 * Completely removes all View wrapper divs so rows go directly into grid containers
 * provided by homepage template (.mel-sticker-wall, .mel-row__scroller).
 * 
 * This template outputs ONLY the rows content - no .view, .view-content, or other wrappers.
 */
#}
{{ rows }}

