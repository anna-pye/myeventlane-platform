{#
/**
 * @file
 * Template for mel_home_events discover display - Sticker Wall grid.
 * 
 * Outputs rows directly without wrapper - homepage template provides .mel-sticker-wall container.
 */
#}
{% if rows %}
  {% for row in rows %}
    {{- row.content -}}
  {% endfor %}
{% endif %}
