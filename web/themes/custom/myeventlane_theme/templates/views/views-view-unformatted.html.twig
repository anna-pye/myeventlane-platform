{#
/**
 * @file
 * Default theme implementation to display a view of unformatted rows.
 *
 * MyEventLane override:
 * - If this is an Event View (node: event), wrap rows in
 *   `.mel-grid.mel-grid--events`.
 * - Otherwise, fall back to core's default unformatted markup.
 */
#}

{% if mel_is_event_view %}
  {% if rows %}
    <div class="mel-grid mel-grid--events">
      {% for row in rows %}
        {{- row.content -}}
      {% endfor %}
    </div>
  {% endif %}
{% else %}
  {% if title %}
    <h3>{{ title }}</h3>
  {% endif %}

  {% for row in rows %}
    {%
      set row_classes = [
        default_row_class ? 'views-row',
      ]
    %}
    <div{{ row.attributes.addClass(row_classes) }}>
      {{- row.content -}}
    </div>
  {% endfor %}
{% endif %}
