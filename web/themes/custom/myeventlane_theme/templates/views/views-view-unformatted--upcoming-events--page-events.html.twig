{#
/**
 * @file
 * Template override for Upcoming Events view events page display.
 *
 * Canonical event grid wrapper - no additional markup.
 */
#}
<!-- MEL DEBUG: EVENTS VIEW UNFORMATTED ACTIVE (views-view-unformatted--upcoming-events--page-events.html.twig) -->
{% if rows %}
  <div class="mel-grid mel-grid--events">
    {% for row in rows %}
      {{- row.content -}}
    {% endfor %}
  </div>
{% endif %}
