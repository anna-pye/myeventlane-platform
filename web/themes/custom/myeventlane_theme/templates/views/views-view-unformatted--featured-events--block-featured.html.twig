{#
/**
 * @file
 * Featured events (homepage) — grid output.
 *
 * IMPORTANT:
 * - Use the canonical `.mel-event-grid` wrapper (defined in components/_event-grid.scss)
 *   to achieve the 4-across desktop grid without introducing new layout markup.
 * - No card markup here; rows already contain themed node output.
 * - When there are 0 rows, output nothing so the homepage section can collapse.
 */
#}

{% if rows %}
  <div class="mel-event-grid" aria-label="{{ 'Featured events'|t }}">
    {% for row in rows %}
      {{- row.content -}}
    {% endfor %}
  </div>
{% endif %}
