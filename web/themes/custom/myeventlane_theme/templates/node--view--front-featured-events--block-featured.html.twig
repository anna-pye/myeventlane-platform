{#
/**
 * @file
 * Event node in Front: Featured Events (block_featured) — editorial magazine hero slide.
 *
 * Uses the same presentation as node--event--editorial-magazine (view model + mel-event-card).
 */
#}
{% include '@myeventlane_theme/components/event-card/mel-event-card.html.twig' with {
  variant: mel_event_card_variant|default('editorial'),
  layout: mel_event_card_layout|default('hero'),
  attributes: attributes,
} %}
