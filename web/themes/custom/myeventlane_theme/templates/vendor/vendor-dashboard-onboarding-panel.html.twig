{% if onboarding_incomplete %}
  <section class="mel-dashboard-invite">
    <h2>Finish setting up your event</h2>
    <p>You're almost there.</p>
    <a href="{{ resume_url }}" class="mel-btn mel-btn--primary">
      Resume setup
    </a>
  </section>
{% endif %}
