{#
/**
 * @file
 * User entity template — login view mode (if used).
 *
 * Do not duplicate the login form markup here; the form uses
 * form--user-login-form.html.twig. Rendering content twice caused duplicate fields.
 */
#}
{% if content %}
  {{- content -}}
{% endif %}
