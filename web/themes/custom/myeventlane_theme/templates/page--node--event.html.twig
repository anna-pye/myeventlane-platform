{#
/**
 * @file
 * Page template override for Event node full page.
 * 
 * Removes container constraints so event-shell can use full width.
 */
#}
{% extends "page.html.twig" %}

{% block content %}
  {# Parent page.html.twig already wraps content in <main id="main-content">; avoid nested mains. #}
  <div class="mel-content mel-content--event-full">
    {{ page.content }}
  </div>
{% endblock %}
