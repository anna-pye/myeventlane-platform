{#
/**
 * @file
 * Canonical customer profile settings shell — Form API underneath, MEL owns chrome.
 *
 * Page shell provides the H1; this template only adds a short intro + form.
 */
#}
{% include '@myeventlane_theme/account/mel-account-settings.html.twig' with {
  intro: intro,
  form: form,
  mel_form_classification: mel_form_classification|default('customer'),
} only %}
