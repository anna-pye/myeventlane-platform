/* Placeholder scanner service worker source.
 * Active scanner SW is served dynamically per event route:
 * /event/{event}/tickets/checkin/sw.js
 */
self.addEventListener('install', function () {});
